# CLI vs Studio benchmark

Confronto controllato di Claude Code tramite:

- CLI: `ccllrun -p --output-format stream-json`
- Studio: `POST /api/claude`, cioe il percorso reale usato dalla chat GUI

I prompt sono in `prompts/`; gli output e il report vengono generati in `results/`.

